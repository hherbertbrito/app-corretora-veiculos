import { CpfMascaradoPipe } from './cpf-mascarado.pipe';

describe('CpfMascaradoPipe', () => {
  it('create an instance', () => {
    const pipe = new CpfMascaradoPipe();
    expect(pipe).toBeTruthy();
  });
});
